spring:
  application:
    name: reservation-service
  datasource:
    url: jdbc:postgresql://156.241.128.83:5432/reservations
    username: program
    password: test
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8070

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: health
      base-path: /manage
  endpoint:
    health:
      show-details: always
